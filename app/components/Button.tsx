import React from "react";

export default function Button({ bg }: any) {
  return (
    <div
      className={`bg-white min-w-[160px] min-h-[48px] flex items-center justify-center`}
    >
      SEE PRODUCT
    </div>
  );
}
